# Port to expose code-server on the host
PORT=8080

# Override the container user (default: keepresearch)
DOCKER_USER=keepresearch

# Absolute path on the host machine to open as the workspace
WORKSPACE_PATH=/path/to/your/projects
